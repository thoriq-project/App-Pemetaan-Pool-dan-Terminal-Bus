<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.poolbismapping">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".LokasiActivity" />
        <activity
            android:name=".TerminalDetailActivity"
            android:theme="@style/AppTheme.TerminalDetail" />
        <activity android:name=".AddTerminalActivity" />
        <activity android:name=".PoolDetailActivity" />
        <activity android:name=".PoolActivity" />
        <activity
            android:name=".KecamatanActivity"
            android:theme="@style/AppTheme.PoolAct" />
        <activity android:name=".Add.AddActivity" />
        <activity android:name=".Add.AddPoolActivity" />
        <activity android:name=".CilandakActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".TerminalActivity" />
        <activity android:name=".MainActivity"/>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_map_api_key" />
    </application>

</manifest>